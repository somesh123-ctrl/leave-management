<% include ./partial/header %> <% include ./partial/studfunction %>

<div
  class="col-sm-6"
  style="    flex: 0 0 50%;
max-width: 50%;
margin-left: 431px;
margin-top: -181px;  text-align: -webkit-center;"
>
  <h1>Apply for leave here <%=student.name%></h1>
  <form action="/student/<%=student._id%>/apply" method="POST">
    <div class="form-group">
      <input
        type="text"
        placeholder="subject"
        name="loan[subject]"
        class="form-control"
      />
    </div>
    <div class="form-group">
      <input
        type="text"
        placeholder="amount"
        name="loan[subject]"
        class="form-control"
      />
    </div>
   

    <div class="form-group">
      <button class="btn btn-lg btn-block btn-default">Submit</button>
    </div>
  </form>

  <% include ./partial/footer %>
</div>
